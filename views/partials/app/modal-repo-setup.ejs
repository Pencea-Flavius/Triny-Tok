<!-- REPO Setup Guide Modal -->
<div id="repoSetupModal" class="modal-overlay" style="display:none;">
  <div class="modal-content glass" style="max-width:550px;">
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:20px;">
      <h3 style="font-size:1.3rem;font-weight:800;margin:0;">R.E.P.O. Mod Setup</h3>
      <button class="btn-secondary btn-sm" id="closeRepoSetupBtn" style="padding:4px;">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>
      </button>
    </div>

    <div style="display:flex;flex-direction:column;gap:16px;font-size:0.95rem;line-height:1.6;">
      <div class="card-tip" style="background:rgba(52,152,219,0.1);border-left-color:var(--accent);">
        <div class="tip-icon" style="color:var(--accent);">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>
        </div>
        <div>
          <div class="tip-title">Requirements</div>
          <div class="tip-desc">The mod requires <b>BepInEx</b> and <b>REPOLib</b> installed in R.E.P.O.</div>
        </div>
      </div>

      <div style="background:var(--bg3);padding:15px;border-radius:10px;border:1px solid var(--border);">
        <div style="font-weight:700;margin-bottom:8px;font-size:0.8rem;text-transform:uppercase;color:var(--dim);">Step 1: Install BepInEx</div>
        <p>Download and install <b>BepInEx</b> for R.E.P.O. Follow the BepInEx installation guide for Unity games.</p>
        <p style="font-size:0.85rem;color:var(--muted);margin-top:6px;">BepInEx goes in the game root folder next to <code>REPO.exe</code>.</p>
      </div>

      <div style="background:var(--bg3);padding:15px;border-radius:10px;border:1px solid var(--border);">
        <div style="font-weight:700;margin-bottom:8px;font-size:0.8rem;text-transform:uppercase;color:var(--dim);">Step 2: Install REPOLib</div>
        <p>Download <b>REPOLib</b> from Thunderstore/Nexus and drop its DLL into <code>BepInEx/plugins/</code>.</p>
      </div>

      <div style="background:var(--bg3);padding:15px;border-radius:10px;border:1px solid var(--border);">
        <div style="font-weight:700;margin-bottom:8px;font-size:0.8rem;text-transform:uppercase;color:var(--dim);">Step 3: Install TrinyTok REPO Mod</div>
        <p>Copy <code>TrinyTokRepo.dll</code> into <code>BepInEx/plugins/</code>.</p>
        <p style="font-size:0.85rem;color:var(--muted);margin-top:6px;">The mod connects to <code>127.0.0.1:51337</code> when the game starts.</p>
      </div>

      <div style="background:var(--bg3);padding:15px;border-radius:10px;border:1px solid var(--border);">
        <div style="font-weight:700;margin-bottom:8px;font-size:0.8rem;text-transform:uppercase;color:var(--dim);">Step 4: Connect</div>
        <p>Click <b>Start Bridge</b> in this dashboard, then launch R.E.P.O. Once in a game the status will turn <b style="color:var(--success);">GREEN</b>.</p>
      </div>

      <button class="btn-primary" id="repoGotItBtn" style="margin-top:10px;">Got it, let's play!</button>
    </div>
  </div>
</div>
